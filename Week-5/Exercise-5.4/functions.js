const functions = {
    add: (num1, num2) => num1 + num2,

    subtract: (nums1, num2) => nums1 - num2,

    multiply: (num1, num2) => num1 * num2,

    divide: (num1, num2) => num1 / num2
}

module.exports = functions;
